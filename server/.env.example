# Copy this file to .env and update the values as needed.
SORA_API_KEY=sk-your-openai-api-key
# Set to "true" to use the built-in mock job runner instead of calling the API.
MOCK_MODE=true
# Optionally change the port the Express server listens on.
PORT=3000
